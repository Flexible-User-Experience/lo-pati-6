{% block trix_form_type_widget %}
    <input id="{{ form.vars.id }}" value="{{ form.vars.value }}" type="hidden" name="{{ form.vars.full_name }}">
    <trix-editor input="{{ form.vars.id }}" style="min-height:{{ trix_height }}"></trix-editor>
{% endblock %}
